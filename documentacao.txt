Visão geral do projeto
O projeto consiste em uma aplicação web para estudo que gera quizzes com auxílio de IA e permite que o usuário tire dúvidas sobre as questões. A aplicação utilizará a API do Gemini para geração de conteúdo e um banco de dados PostgreSQL para cadastro, autenticação e persistência do histórico de quizzes. O frontend será desenvolvido em React com JavaScript.

Escopo e regras principais
Cada quiz deve conter exatamente 10 perguntas (regra fixa).
O usuário poderá solicitar explicações/"dúvidas" sobre as perguntas (funcionalidade opcional por pergunta, mas disponível no sistema).
O sistema deve exibir as fontes usadas para construir o quiz e deve privilegiar fontes confiáveis (ex.: sites institucionais, artigos científicos).

Requisitos funcionais (RF)
RF01 — Cadastro de usuário: O sistema deve permitir que um usuário crie uma conta informando, no mínimo, e-mail (ou usuário) e senha, validando campos obrigatórios.

RF02 — Login e autenticação: O sistema deve permitir login com credenciais válidas e manter a sessão do usuário autenticado para acesso às funcionalidades do quiz.​

RF03 — Geração de quiz (10 perguntas): O sistema deve permitir que o usuário informe um tema e, a partir dele, gerar um quiz contendo exatamente 10 perguntas relacionadas ao tema.
​
RF04 — Persistência do histórico: O sistema deve salvar no banco de dados os últimos 3 quizzes gerados por usuário, incluindo pelo menos: tema, lista de perguntas, data/hora de criação e fontes.

RF05 — Consulta de histórico: O sistema deve permitir que o usuário visualize os últimos 3 quizzes salvos e selecione um deles para revisão.
​
RF06 — Perguntar sobre uma pergunta: Em cada pergunta do quiz, o sistema deve permitir que o usuário envie uma pergunta de esclarecimento (ex.: "por que essa alternativa é correta?") e receba uma resposta da IA vinculada à questão.
​
RF07 — Exibição de fontes: O sistema deve apresentar uma aba para as fontes utilizadas para montar o quiz (por pergunta e/ou por quiz), exibindo ao menos título/descrição e link.
​
RF08 — Obtenção de respostas do quiz: O sistema deve gerar e apresentar as respostas das perguntas do quiz (por exemplo: gabarito e/ou explicação curta), vinculando cada resposta à respectiva pergunta.
​
RF09 — Critério de confiabilidade das fontes: O sistema deve priorizar fontes confiáveis (ex.: domínios institucionais, publicações científicas e repositórios acadêmicos) ao montar o quiz e ao justificar respostas.
​
RF10 — Logout, o sistema deve permitir que o usuário faça logout e retorne para a tela de login.

RF11 — Mensagem de erro no login, o sistema deve mostrar uma mensagem quando o usuário informar dados incorretos.
Requisitos não funcionais (RNF)

RNF01 — Usabilidade: A interface deve ser simples e permitir gerar um quiz em poucos passos.
​
RNF02 — Desempenho: O sistema deve responder às ações principais (login, abrir histórico e exibir quiz já salvo) em tempo adequado, login e cadastro no máximo 3 segundos e gerar o quis no máximo 20 segundos.

RNF03 — Segurança: Senhas devem ser armazenadas de forma segura (hash), e a aplicação deve proteger rotas autenticadas e dados do usuário.(opcional/se houver tempo)
​

Tecnologias(não sei se utilizaremos essas tecnologias ainda)
Frontend: React + JavaScript.
Backend/API: recomendado um backend (ex.: Node/Typescript) para intermediar autenticação, banco e chamadas à API do Gemini.
Banco de dados: PostgreSQL.
IA: Gemini API (Google AI for Developers).


Processos
O usuario realiza cadastro informando e-mail e senha, e o sistema valida e armazena os dados com segurança (RF01, RNF03). Em seguida, realiza login com credenciais validas, iniciando uma sessao autenticada (RF02, RF11, RNF02, RNF03).
Apos autenticado, o usuario informa um tema para estudo e solicita a geracao do quiz. O backend envia a solicitacao para a API do Gemini, que retorna exatamente 10 perguntas com respostas e fontes confiaveis (RF03, RF08, RF09, RNF02). O tempo maximo para geracao deve ser de ate 20 segundos (RNF02).
O sistema salva no PostgreSQL o quiz gerado (tema, perguntas, data/hora e fontes), mantendo apenas os ultimos 3 quizzes por usuario (RF04). O usuario pode acessar e revisar quizzes anteriores pelo historico (RF05, RNF01).
Em cada pergunta, o usuario pode enviar uma duvida, que sera respondida pela IA com base na questao e nas fontes utilizadas (RF06, RF07). O sistema exibe as fontes vinculadas ao quiz para garantir transparencia e confiabilidade (RF07, RF09).
Por fim, o usuario pode realizar logout, encerrando sua sessao e retornando a tela de login (RF10, RNF03).


DOD

1. Atende 100% dos criterios de aceitacao.

2. Regra das 10 perguntas validada.

3. Historico limitado aos ultimos 3 quizzes.

4. Fontes exibidas corretamente.

5. Perguntas de esclarecimento funcionando por questao.


DOR

1. Banco de dados Postgres, pronto para realizar os testes;

2. API do GEMINI(APIKEY) pronta para utilizar;

3. Ambiente de desenvolvimento pronto para iniciar.


